# ============================================
# Restaurant Catalog Service - Deployment + Service
# ============================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-catalog-service
  namespace: delivereats
  labels:
    app: restaurant-catalog-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-catalog-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: restaurant-catalog-service
    spec:
      containers:
        - name: restaurant-catalog-service
          image: us-central1-docker.pkg.dev/usac-sa-201346084/delivereats/restaurant-catalog-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50052
          envFrom:
            - configMapRef:
                name: delivereats-config
            - secretRef:
                name: delivereats-secrets
          env:
            - name: PORT
              value: "50052"
            - name: DB_NAME
              value: "restaurant_db"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            tcpSocket:
              port: 50052
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 50052
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: restaurant-catalog-service
  namespace: delivereats
  labels:
    app: restaurant-catalog-service
spec:
  selector:
    app: restaurant-catalog-service
  ports:
    - port: 50052
      targetPort: 50052
      protocol: TCP
  type: ClusterIP
