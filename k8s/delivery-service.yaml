# ============================================
# Delivery Service - Deployment + Service
# ============================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-service
  namespace: delivereats
  labels:
    app: delivery-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: delivery-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: delivery-service
    spec:
      containers:
        - name: delivery-service
          image: us-central1-docker.pkg.dev/usac-sa-201346084/delivereats/delivery-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50054
          envFrom:
            - configMapRef:
                name: delivereats-config
            - secretRef:
                name: delivereats-secrets
          env:
            - name: PORT
              value: "50054"
            - name: DB_NAME
              value: "delivery_db"
            - name: ORDER_SERVICE_HOST
              value: "order-service:50053"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            tcpSocket:
              port: 50054
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 50054
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: delivereats
  labels:
    app: delivery-service
spec:
  selector:
    app: delivery-service
  ports:
    - port: 50054
      targetPort: 50054
      protocol: TCP
  type: ClusterIP
